name: jankkiller_devtools
description: DevTools extension for JankKiller screen-flow performance profiling.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter
  devtools_extensions: ^0.3.0
  devtools_app_shared: ^0.3.0
  vm_service: ^14.0.0
  fl_chart: ^0.69.0
  uuid: ^4.5.1
  web: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
